## 1. Error Component

- [x] 1.1 Change icon container from `rounded-full` to square (`rounded-none`), keep `bg-destructive/10 text-destructive` tokens
- [x] 1.2 Update "Try Again" button to `rounded-none border border-border bg-background` style
- [x] 1.3 Update "← Home" link to `rounded-none` style matching the button pattern

## 2. Not-Found Component

- [x] 2.1 Replace Indonesian copy with English: description → "The page you're looking for doesn't exist.", link → "← Back to Home"
- [x] 2.2 Update back-to-home anchor to `rounded-none border border-border` style

## 3. Pending Component

- [x] 3.1 Replace raw inline SVG spinner with `Spinner` imported from `@workspace/ui`
